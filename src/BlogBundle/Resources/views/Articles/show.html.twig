{% extends 'base.html.twig' %}
{% block title %}Blog - {{ article.title }}{% endblock %}
{% block body %}
    <div class="container">
        <div class="row">
            <h1>{{ article.title }}</h1>
            <h2>{{ article.content }}</h2>
        </div>
    </div>
{% endblock %}