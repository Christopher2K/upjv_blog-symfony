{{ form_start(myForm) }}
    {{ form_errors(myForm) }}
<table>
    <tr>
        <td>{{ form_row(myForm.title) }}</td>
        <br>
    </tr>
    <tr>
        <td>{{ form_row(myForm.content) }}</td>
    </tr>
    <tr>
        <td>{{ form_row(myForm.themes) }}</td>
    </tr>
    <tr>
        <td>{{ form_row(myForm.submit) }}</td>
    </tr>


</table>
{{ form_end(myForm) }}